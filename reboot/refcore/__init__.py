"""Reference skeleton for viki state management, against REQUIREMENTS_v2.md.

Nothing here is implemented. Data types are real so a failing test fails inside
the function under test rather than in its own setup -- v1's suite had 81% of
tests dying in the Store constructor, never reaching what they named
(FINDINGS.md C-1).
"""
