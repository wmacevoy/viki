# OS / editor junk
.DS_Store
*~
\#*\#
.\#*

# Fossil checkout artifacts (in case a fossil checkout is mixed with this git tree)
*.fossil
*.efossil
_FOSSIL_
.fslckout
manifest
manifest.uuid

# Local dev convenience scripts (host-specific: hostnames, logins, port forwards)
connect

# Build artifacts
build/dist/
build/obj/
.viki/
vendor/fossil-see/vendor/libressl-cache/
vendor/fossil-see/vendor/libressl-build-out/
vendor/download-cache/

# wasm build output. edge/dist/cache.db in particular is a COMPLETE PLAINTEXT
# COPY OF THE CORPUS (QUEUE 35) -- committing it would publish everything the
# cache holds to anyone who can read the git repo.
edge/dist/
# Vendored wasm build inputs: a 2MB LibreSSL static lib, SQLCipher's 9MB
# amalgamation and the openssl headers. All reproducible by
# edge/build-wasm-sqlcipher.sh from the sibling repos; none of it is ours.
edge/vendor-wasm/
# generated PWA icons are checked IN (they are small and part of the app);
# only the built/pulled artefacts stay out.
core/build/
.claude/
